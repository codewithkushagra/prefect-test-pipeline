# Prefect configuration file
name: prefect-test-pipeline
prefect-version: 2.14.0

# Deployments
deployments:
  - name: sequential-workflow
    entrypoint: workflow.py:sequential_workflow
    tags:
      - demo
      - sequential
      - basic
    description: "A sequential workflow with 3 tasks"
    version: "1.0.0"
